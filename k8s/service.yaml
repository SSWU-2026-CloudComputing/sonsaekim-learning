apiVersion: v1
kind: Service
metadata:
  name: learning-service
  annotations:
    cloud.google.com/backend-config: '{"default": "learning-backend-config"}'
spec:
  type: ClusterIP
  selector:
    app: learning-service

  ports:
    - name: http
      port: 3000
      targetPort: 3000